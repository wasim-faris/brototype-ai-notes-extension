{
  "name": "brototype-ai-notes",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --env-file-if-exists=.env build.js",
    "build:release": "node --env-file-if-exists=.env build.js --release",
    "dev": "node --env-file-if-exists=.env build.js --watch",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^30.0.1",
    "vite": "^5.4.11"
  }
}
